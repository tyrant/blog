- content_for :right_column do
  - link = comfy_admin_blog_post_revisions_path(@site, @post)
  = render "comfy/admin/cms/revisions/sidebar", record: @post, link: link
  .mt-3
    = button_to "Backfill this post’s notes", comfy_admin_substack_blizzard_backfill_post_path, method: :post, params: { post_id: @post.id }, class: "btn btn-outline-secondary btn-sm btn-block", data: { confirm: "Backfill this post’s Substack notes now?" }

= render "comfy/admin/blog/partials/post_navigation"

.page-header
  %h2= t('.title')

= comfy_form_with model: @post, scope: :post, url: {action: :update}, html: {multipart: true} do |form|
  = render form
